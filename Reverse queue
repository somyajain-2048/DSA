#include<iostream>
#include<queue>
#include<stack>
using namespace std;
stack<int>st;
int n = q.size();
while(!q.empty()){
  st.push(q.front());
  q.pop();
  }

while(!st.empty()){
  q.push(st.top());
  st.pop();
  
}
