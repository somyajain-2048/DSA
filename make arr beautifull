 void beutyarr(vector<int>&arr){
stack<int>st;
  
 for(int i =0 i<arr.size();i++){
 	if(st.empty()){
		st.push(arr[i]);
	}

  else if(arr[i]>=0){
  if(st.top()>=0)
	st.push(arr[i]);

	else
 	st.pop();
 	}
